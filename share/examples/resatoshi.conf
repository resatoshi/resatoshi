# ReSatoshi public alpha testnet example
# Alpha RST has no monetary value and the network may be reset.

testnet=1
server=1
listen=1

# The first public node address will be announced separately. Until then,
# start the first node without addnode and add peers explicitly on later nodes:
# addnode=203.0.113.10:49595

# RPC remains local-only by default. Do not expose port 49594 to the internet.
